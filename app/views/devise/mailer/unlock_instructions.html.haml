%p
  = t('devise.views.hello', email: @resource.email)
%p
  = t('devise.views.phrases.account_has_been_locked')
%p
  = t('devise.views.phrases.click_link')
%p
  = link_to t('devise.views.unlock_my_account'), unlock_url(@resource, :unlock_token => @token)